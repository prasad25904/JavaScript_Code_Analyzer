#include "ast.h"
#include "symbol_table.h"  // 👈 Include this instead of redefining

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// You can now use add_symbol(), find_symbol(), print_symbol_table() here
// No need to define them again
